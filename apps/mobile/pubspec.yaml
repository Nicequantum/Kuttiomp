name: kuttiomp_mobile
description: Kuttiomp — Narragansett Language Revitalization Mobile App
publish_to: "none"
version: 2.3.0+1

# MAD v2.0 §12: Flutter ^3.24.0 track (Dart 3.5+). Freezed: hand-written models — see lib/config/FREEZED_DECISION.md
environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  flutter_dotenv: ^5.2.1
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.5.1
  supabase_flutter: ^2.8.0
  isar: ^4.1.0
  isar_flutter_libs: ^4.1.0
  go_router: ^14.6.2
  path_provider: ^2.1.5
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  riverpod_generator: ^2.5.1
  isar_generator: ^4.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/
    - assets/audio/
    - l10n/